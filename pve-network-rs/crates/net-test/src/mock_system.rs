//! Mock system for testing

/// Mock system for testing network operations
pub struct MockSystem {
    // TODO: Add mock system implementation
}

impl MockSystem {
    /// Create new mock system
    pub fn new() -> Self {
        Self {}
    }
}

impl Default for MockSystem {
    fn default() -> Self {
        Self::new()
    }
}
